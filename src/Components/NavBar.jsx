import style from '../CSS/NavBar.module.css'

const NavBar = () => {
  return <nav id={style.NavBar} >
        <div className='flex'>
            <h2>Wellness Retreats</h2>
        </div>
  </nav>
}

export default NavBar
